<UserControl x:Class="StudentManagement.Desktop.Views.AdmissionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:StudentManagement.Desktop.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="900"
             d:DataContext="{d:DesignInstance Type=vm:AdmissionViewModel}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolVis"/>

        <!-- Label style -->
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <!-- TextBox style -->
        <Style x:Key="FieldBox" TargetType="TextBox">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Padding="{TemplateBinding Padding}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox style -->
        <Style x:Key="FieldCombo" TargetType="ComboBox">
            <Setter Property="Height" Value="38"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- Primary button -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Height" Value="42"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1D4ED8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#93C5FD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary button -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Height" Value="42"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="#F1F5F9"/>
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E2E8F0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ── Page Header ─────────────────────────────────────────────── -->
        <Border Grid.Row="0"
                Background="White"
                BorderBrush="#E2E8F0"
                BorderThickness="1"
                CornerRadius="10"
                Padding="24,20"
                Margin="0,0,0,24">
            <StackPanel>
                <TextBlock Text="Tiếp nhận học sinh"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="#0F172A"/>
                <TextBlock Text="Biểu mẫu 1 — Lập hồ sơ và phân lớp học sinh mới"
                           FontSize="13"
                           Foreground="#64748B"
                           Margin="0,6,0,0"/>
            </StackPanel>
        </Border>

        <!-- ── Form Area ───────────────────────────────────────────────── -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="24"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Loading overlay -->
                <Border Grid.Column="0" Grid.ColumnSpan="3"
                        Background="#80FFFFFF"
                        Panel.ZIndex="20"
                        Visibility="{Binding IsBusy, Converter={StaticResource BoolVis}}">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <ProgressBar IsIndeterminate="True" Width="200" Height="8"/>
                        <TextBlock Text="Đang xử lý..."
                                   Margin="0,12,0,0"
                                   FontWeight="SemiBold"
                                   Foreground="#475569"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- ── Left Column ─────────────────────────────────────── -->
                <Border Grid.Column="0"
                        Background="White"
                        BorderBrush="#E2E8F0"
                        BorderThickness="1"
                        CornerRadius="10"
                        Padding="28">
                    <StackPanel>
                        <TextBlock Text="Thông tin cá nhân"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Foreground="#1E293B"
                                   Margin="0,0,0,20"/>

                        <!-- Họ và tên -->
                        <TextBlock Text="Họ và tên *" Style="{StaticResource FieldLabel}"/>
                        <TextBox Text="{Binding Ten, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource FieldBox}"
                                 Margin="0,0,0,16"/>

                        <!-- Ngày sinh -->
                        <TextBlock Text="Ngày sinh *" Style="{StaticResource FieldLabel}"/>
                        <DatePicker SelectedDate="{Binding NgaySinh, UpdateSourceTrigger=PropertyChanged}"
                                    Height="38"
                                    FontSize="14"
                                    BorderBrush="#D1D5DB"
                                    Margin="0,0,0,16"/>

                        <!-- Giới tính -->
                        <TextBlock Text="Giới tính *" Style="{StaticResource FieldLabel}"/>
                        <ComboBox SelectedItem="{Binding GioiTinh, UpdateSourceTrigger=PropertyChanged}"
                                  Style="{StaticResource FieldCombo}"
                                  Margin="0,0,0,16">
                            <ComboBox.Items>
                                <sys:String xmlns:sys="clr-namespace:System;assembly=mscorlib">Nam</sys:String>
                                <sys:String xmlns:sys="clr-namespace:System;assembly=mscorlib">Nữ</sys:String>
                            </ComboBox.Items>
                        </ComboBox>

                        <!-- Địa chỉ -->
                        <TextBlock Text="Địa chỉ *" Style="{StaticResource FieldLabel}"/>
                        <TextBox Text="{Binding DiaChi, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource FieldBox}"
                                 Margin="0,0,0,16"/>

                        <!-- Email -->
                        <TextBlock Text="Email" Style="{StaticResource FieldLabel}"/>
                        <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource FieldBox}"
                                 Margin="0,0,0,0"/>
                    </StackPanel>
                </Border>

                <!-- ── Right Column ────────────────────────────────────── -->
                <Border Grid.Column="2"
                        Background="White"
                        BorderBrush="#E2E8F0"
                        BorderThickness="1"
                        CornerRadius="10"
                        Padding="28">
                    <StackPanel>
                        <TextBlock Text="Phân lớp"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Foreground="#1E293B"
                                   Margin="0,0,0,20"/>

                        <!-- Lớp -->
                        <TextBlock Text="Chọn lớp *" Style="{StaticResource FieldLabel}"/>
                        <ComboBox ItemsSource="{Binding ClassList}"
                                  SelectedItem="{Binding SelectedClass}"
                                  DisplayMemberPath="TenLop"
                                  Style="{StaticResource FieldCombo}"
                                  Margin="0,0,0,12"/>

                        <!-- Sĩ số hiện tại -->
                        <Border Background="#F0F9FF"
                                BorderBrush="#BAE6FD"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="14,10"
                                Margin="0,0,0,24"
                                Visibility="{Binding SelectedClass, Converter={StaticResource BoolVis}}">
                            <StackPanel>
                                <TextBlock Text="Sĩ số hiện tại"
                                           FontSize="12"
                                           Foreground="#0369A1"
                                           FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding SelectedClass.SiSo, StringFormat='{}{0} học sinh'}"
                                           FontSize="22"
                                           FontWeight="Bold"
                                           Foreground="#0284C7"
                                           Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- ── Regulation info box ─────────────────────── -->
                        <Border Background="#FFFBEB"
                                BorderBrush="#FDE68A"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="14,12"
                                Margin="0,0,0,24">
                            <StackPanel>
                                <TextBlock Text="Quy định hiện hành"
                                           FontSize="12"
                                           FontWeight="Bold"
                                           Foreground="#92400E"
                                           Margin="0,0,0,8"/>
                                <TextBlock Text="{Binding RegulationSummary}"
                                           FontSize="12"
                                           Foreground="#78350F"
                                           TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>

                        <!-- ── Status message ──────────────────────────── -->
                        <Border CornerRadius="8"
                                Padding="14,12"
                                Margin="0,0,0,24"
                                Visibility="{Binding StatusMessage, Converter={StaticResource BoolVis}}">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="#FEE2E2"/>
                                    <Setter Property="BorderBrush" Value="#FECACA"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsSuccess}" Value="True">
                                            <Setter Property="Background" Value="#DCFCE7"/>
                                            <Setter Property="BorderBrush" Value="#BBF7D0"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="{Binding StatusMessage}"
                                       TextWrapping="Wrap"
                                       FontSize="13"
                                       FontWeight="SemiBold">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#DC2626"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsSuccess}" Value="True">
                                                <Setter Property="Foreground" Value="#16A34A"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Border>

                        <!-- ── Action Buttons ──────────────────────────── -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Button Grid.Column="0"
                                    Content="Làm mới"
                                    Command="{Binding ResetFormCommand}"
                                    Style="{StaticResource SecondaryButton}"/>

                            <Button Grid.Column="2"
                                    Content="Tiếp nhận"
                                    Command="{Binding SubmitCommand}"
                                    Style="{StaticResource PrimaryButton}"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
