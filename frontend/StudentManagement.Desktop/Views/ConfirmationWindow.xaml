<Window x:Class="StudentManagement.Desktop.Views.ConfirmationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Xác nhận yêu cầu" 
        Height="220" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Border CornerRadius="12" Background="White" BorderBrush="#E2E8F0" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="1" Color="#0F172A" Opacity="0.12"/>
        </Border.Effect>
        
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Title / Icon -->
                <RowDefinition Height="*"/>    <!-- Message / Checkbox -->
                <RowDefinition Height="Auto"/> <!-- Buttons -->
            </Grid.RowDefinitions>

            <!-- Title / Icon -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                <TextBlock Text="❓" FontSize="20" Margin="0,0,8,0" VerticalAlignment="Center"/>
                <TextBlock Text="Xác nhận hành động" FontSize="16" FontWeight="Bold" Foreground="#1E293B" VerticalAlignment="Center"/>
            </StackPanel>

            <!-- Message / Checkbox -->
            <StackPanel Grid.Row="1">
                <TextBlock x:Name="TxtMessage" 
                           Text="Bạn có chắc muốn thực hiện thao tác này không?" 
                           FontSize="14" Foreground="#475569" 
                           TextWrapping="Wrap" Margin="0,0,0,12"/>
                
                <CheckBox x:Name="ChkDoNotAskAgain" 
                          Content="Không hỏi lại lần sau" 
                          FontSize="12" Foreground="#64748B" 
                          FontWeight="Medium" Cursor="Hand"/>
            </StackPanel>

            <!-- Buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                <!-- Cancel Button -->
                <Button Content="Hủy bỏ" 
                        Click="Cancel_Click"
                        Width="90" Height="34"
                        Background="#F1F5F9" Foreground="#475569"
                        BorderBrush="#CBD5E1" BorderThickness="1"
                        FontWeight="SemiBold" FontSize="13"
                        Margin="0,0,8,0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <!-- Confirm Button -->
                <Button Content="Đồng ý" 
                        Click="Confirm_Click"
                        Width="90" Height="34"
                        Background="#2563EB" Foreground="White"
                        BorderThickness="0"
                        FontWeight="SemiBold" FontSize="13"
                        Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
